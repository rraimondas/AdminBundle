{% import 'SyliusUiBundle:Macro:headers.html.twig' as headers %}

<div class="ui stackable two column grid">
    <div class="column">
        {{ headers.default(header|trans, configuration.vars.icon|default('plus'), configuration.vars.subheader|default(null)|trans) }}

        {% include configuration.vars.templates.breadcrumb|default('@PlatformAdmin/Crud/Create/_breadcrumb.html.twig') %}
    </div>
    <div class="middle aligned column">
        {% include configuration.vars.templates.toolbar|default('@PlatformAdmin/Crud/_toolbar.html.twig') ignore missing %}
    </div>
</div>
